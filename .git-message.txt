feat(create-kost): strict geofencing, reverse geocode, and UI feedbacks

- Added client-side geofencing bounds check (disables submit button if pin is outside the selected district bounds)
- Implemented reverse geocoding on map marker drag with debounce to auto-update/detect district based on exact coordinates
- Enhanced Google Maps `address_components` parsing logic by checking levels in priority order (administrative_area_level_3 to sublocality)
- Added visual feedback UI for auto-detected district messages and out-of-bounds map warnings
- Added intelligent map zooming based on location geometry type (rooftop vs approximate)
